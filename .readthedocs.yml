version: 2

sphinx:
    configuration: docs/conf.py

python:
   version: 3.9
   install:
      - requirements: requirements.txt
      - requirements: docs/requirements.txt
      - method: pip
        path: .
